---
status: skip
title: "Frontend Restyle to Match Demo — Backend"
requirement: "frontend 畫面可以照 demo 的樣式做修改 (restyle frontend screens to match the demo/ prototype's visual style)"
---

# Frontend Restyle to Match Demo — Backend Spec

## Overview
This requirement is purely a visual/layout restyle of the existing frontend (see `specs/frontend/frontend-demo-restyle.md`). It reuses the existing API contracts (`GET/POST/PUT/DELETE /api/currencies`) exactly as-is — no new endpoint, field, or response shape is needed.

## Requirements
No changes required.

## Implementation Details
No changes required.

## Acceptance Criteria
- [x] No changes required
