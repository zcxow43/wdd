# Relative/empty base URL: requests go through the Vite dev server proxy (see vite.config.ts),
# which forwards /api/* to VITE_BACKEND_PROXY_TARGET. This avoids CORS in dev.
VITE_API_BASE_URL=

# Backend the dev server proxies /api/* requests to.
VITE_BACKEND_PROXY_TARGET=http://localhost:8080
